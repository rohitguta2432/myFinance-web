---
phase: 02-assessment-steps-1-3
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - src/hooks/assessment/useFinancials.ts
  - src/app/(protected)/assessment/step-2/page.tsx
autonomous: true
requirements: [ASSESS-02, ASSESS-08, ASSESS-09, ASSESS-10]

must_haves:
  truths:
    - "User can add income sources (type, amount, frequency, TDS toggle)"
    - "User can add expense categories (type, amount, frequency, essential/discretionary)"
    - "User can edit and delete existing income and expense items"
    - "Cash flow summary card shows monthly income, expenses, EMIs, surplus, and savings rate"
    - "EMI mismatch warning appears when expense EMIs differ from liabilities EMI total"
    - "User can navigate Back to Step 1 and Next to Step 3"
  artifacts:
    - path: "src/hooks/assessment/useFinancials.ts"
      provides: "React Query hooks for income/expense CRUD"
      exports: [useFinancialsQuery, useAddIncomeMutation, useAddExpenseMutation, useDeleteIncomeMutation, useDeleteExpenseMutation, useUpdateIncomeMutation, useUpdateExpenseMutation]
    - path: "src/app/(protected)/assessment/step-2/page.tsx"
      provides: "Step 2 page — income & expenses with modal add/edit"
  key_links:
    - from: "src/app/(protected)/assessment/step-2/page.tsx"
      to: "src/hooks/assessment/useFinancials.ts"
      via: "all CRUD mutations"
    - from: "src/hooks/assessment/useFinancials.ts"
      to: "src/lib/assessment-api.ts"
      via: "addIncome, addExpense, deleteIncome, deleteExpense, updateIncome, updateExpense"
---

<objective>
Port Step 2 (Income & Expenses) with full CRUD — add/edit/delete income sources and expense categories — plus the cash flow summary card.

Purpose: Users can build a complete picture of their monthly cash flow before advancing to assets/liabilities.

Output: useFinancials.ts hook and Step 2 page.tsx with modal add/edit, list rendering, and cash flow summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

Phase 1 infrastructure:
- `src/lib/api-client.ts` — `api.get/post/put/delete` helpers
- `src/store/useAssessmentStore.ts` — Zustand store with `incomes`, `addIncome`, `removeIncome`, `updateIncome`, `expenses`, `addExpense`, `removeExpense`, `updateExpense`, `liabilities`
- `src/lib/assessment-api.ts` (created in Plan 01) — all API functions with DTO mappers

Plan 01 creates:
- `src/components/assessment/step-navigation.tsx` — StepNavigation component
- `src/lib/assessment-api.ts` — getFinancials, addIncome, addExpense, etc.

Inline styles convention — NO Tailwind classes in this file.
Colors: bg #0F172A, card #0F172A with border rgba(255,255,255,0.05), text #F1F5F9/#CBD5E1/#94A3B8, accent #10B981, red #EF4444.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useFinancials hooks</name>
  <files>src/hooks/assessment/useFinancials.ts</files>
  <action>
Create `src/hooks/assessment/useFinancials.ts` — port of useFinancials.js:

```ts
"use client";
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { getFinancials, addIncome, addExpense, deleteIncome, deleteExpense, updateIncome, updateExpense } from "@/lib/assessment-api";

export const useFinancialsQuery = () =>
    useQuery({ queryKey: ["financials"], queryFn: getFinancials, staleTime: 5 * 60 * 1000 });

// One mutation per operation: useAddIncomeMutation, useAddExpenseMutation,
// useDeleteIncomeMutation, useDeleteExpenseMutation,
// useUpdateIncomeMutation, useUpdateExpenseMutation
// Each invalidates ["financials"] on success.
```

Export all 7 hooks.
  </action>
  <verify>
    <automated>cd /home/t0266li/Documents/myFinance-web && source ~/.nvm/nvm.sh && nvm use 20 && npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <done>useFinancials.ts exports all 7 hooks with correct TypeScript types</done>
</task>

<task type="auto">
  <name>Task 2: Create Step 2 page</name>
  <files>src/app/(protected)/assessment/step-2/page.tsx</files>
  <action>
Create `src/app/(protected)/assessment/step-2/page.tsx` — port of Step2IncomeExpenses.jsx.

"use client" at top.

Key implementation:

**Data layer:**
- `const { data: financialsData, isLoading: isFetchingFinancials } = useFinancialsQuery();`
- All 6 CRUD mutations imported from `@/hooks/assessment/useFinancials`
- Hydrate Zustand store from API in useEffect when financialsData changes
- Cross-validate EMIs with liabilities from store

**State:**
- `isModalOpen`, `modalType` ('income' | 'expense'), `editingId`
- Form fields: `category`, `amount`, `frequency`, `isEssential`, `taxDeducted`, `tdsPercentage`, `description`

**INCOME_EMOJI and EXPENSE_EMOJI lookup maps** — port exactly from source.

**getIncomeDisplay / getExpenseDisplay helpers** — strip emoji prefix from stored names.

**openModal(type, item?)** — sets form state for add or edit mode.

**handleSave()** — optimistic update pattern:
- Income: add to store, call API, revert on error + toast.error
- Expense: same pattern
- Edit: update store, call API, revert on error

**calculateMonthly(item)** — converts frequency to monthly equivalent.

**Derived values:**
- `totalMonthlyIncome`, `totalMonthlyExpenses`, `totalMonthlyEMIs` (filter EMI_CATEGORY)
- `surplus`, `savingsRate`
- `emiMismatch` — compare EMI expenses vs liabilities store

**Layout (inline styles, no Tailwind):**

Page header: "Your Cash Flow Reality Check" h1 + subtitle.

Income section:
- List: each item as card with emoji icon, source name, frequency, TDS badge, amount, edit (Pencil) + delete (X) buttons
- "Add Income Source" dashed border button

Expense section:
- Same pattern with red emoji icons
- "Add Expense" dashed border button

Cash flow summary card (shows when incomes.length > 0 && expenses.length > 0):
- Premium card with green top gradient bar
- Income / Expenses / EMIs / SURPLUS / Savings Rate rows
- Green or red feedback based on savings rate >= 20%

EMI mismatch warning (AlertTriangle icon, red card) — includes link to Step 3.

**Bottom nav:** `<StepNavigation step={2} backPath="/assessment/step-1" onNext={() => { /* validate then router.push step-3 */ }} />`
Next validation: totalMonthlyIncome must be > 0.

**Modal (fixed overlay):**
- Backdrop blur overlay
- Bottom sheet on mobile, centered on sm+
- Income fields: category select (with emojis), amount, description, frequency, TDS toggle + TDS percentage
- Expense fields: category select (with emojis), amount, description, frequency, essential/discretionary toggle
- Save/Update button

Use inline styles throughout. Card background: `#0F172A`. Input background: `#0B0F1A`. Border: `rgba(255,255,255,0.1)`.

Use `import { useAssessmentStore } from "@/store/useAssessmentStore"`.
Use `import { StepNavigation } from "@/components/assessment/step-navigation"`.
Use `import toast from "react-hot-toast"`.
  </action>
  <verify>
    <automated>cd /home/t0266li/Documents/myFinance-web && source ~/.nvm/nvm.sh && nvm use 20 && npx tsc --noEmit --skipLibCheck 2>&1 | head -30</automated>
  </verify>
  <done>Step 2 page compiles; shows income list, expense list, cash flow card, and modal</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/proxy | JWT httpOnly cookie forwarded automatically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Income/expense amounts | accept | Backend validates numeric types; frontend parseFloat guards NaN |
| T-02-05 | Information Disclosure | Expense categories in store | accept | No sensitive PII beyond financial amounts — acceptable for wizard continuity |
</threat_model>

<verification>
1. TypeScript compiles: `npx tsc --noEmit --skipLibCheck`
2. `src/hooks/assessment/useFinancials.ts` exports 7 hooks
3. `src/app/(protected)/assessment/step-2/page.tsx` has "use client" and all sections
</verification>

<success_criteria>
- Step 2 page renders income list, expense list, and cash flow summary
- Modal opens for add/edit with correct fields for income vs expense
- Delete removes item from store and calls API
- Savings rate card shows green/red feedback
- StepNavigation connects to steps 1 and 3
</success_criteria>

<output>
After completion, create `.planning/phases/02-assessment-steps-1-3/02-02-SUMMARY.md`
</output>
